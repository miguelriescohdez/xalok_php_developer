{% extends 'base.html.twig' %}

{% block title %}Edit Vehicles{% endblock %}

{% block body %}
<div class="form-body">
    <div class="row">
        <div class="form-holder">
            <div class="form-content">
                <div class="form-items">
                    <div class="d-flex align-items-center justify-content-between my-4">
                        <h3 class="m-0">Edit Vehicle</h3>
                        {{ include('vehicles/_delete_form.html.twig') }}
                    </div>
                    {{ include('vehicles/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
                <div class="d-flex align-items-center justify-content-center my-4">
                    <a class="btn btn-primary" href="{{ path('app_vehicles_index') }}">back to list</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
